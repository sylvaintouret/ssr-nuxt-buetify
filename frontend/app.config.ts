import { navItem } from '~~/models/nav';

// This has to be activated for protected routes using definePageMeta({middleware: ["auth"]}) on each page

export default defineAppConfig({
    title: 'Hello Nuxt',
    app: {}

})